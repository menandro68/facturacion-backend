const { Pool } = require("pg"); const pool = new Pool({ connectionString: process.env.DATABASE_URL, ssl: { rejectUnauthorized: false } }); (async () => { await pool.query("ALTER TABLE tenants ADD COLUMN IF NOT EXISTS features JSONB DEFAULT '{}'"); await pool.query("UPDATE tenants SET features = '{\"responsive\": true}' WHERE id = '3d25f1c6-7a34-476f-a74d-f8b28e420a7c'"); const r = await pool.query("SELECT nombre, features FROM tenants ORDER BY nombre"); console.log(JSON.stringify(r.rows, null, 2)); pool.end(); })();
